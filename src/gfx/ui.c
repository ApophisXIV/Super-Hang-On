#include "ui.h"
#include "../../inc/figuras.h"

// TODO: WIP (Work In Progress)
imagen_t *str_to_text_img(const char *str, uint16_t *buf_figs) {

    if (str == NULL || buf_figs) return NULL;

    for (size_t i = 0; str[i] != '\0'; i++) {
    }

    return NULL;
}